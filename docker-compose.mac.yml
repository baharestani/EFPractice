version: '3'

services:
  efpractice:
    image: efpractice
    build:
      context: .
      dockerfile: EFPractice/Dockerfile
    environment:
      ConnectionString: "Server=localhost;Database=test;Uid=root;Pwd=sc4m@th;"